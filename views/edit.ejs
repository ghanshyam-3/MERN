<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Task</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen flex justify-center items-start p-6">

  <div class="w-full max-w-md bg-white shadow-lg rounded-lg p-6">
    
    <!-- Back Link -->
    <a href="/" class="inline-block text-blue-600 hover:underline mb-4">&larr; Go Back to Home</a>
    
    <!-- Edit Form -->
    <form action="/edit" method="post" class="flex flex-col gap-4">
      
      <!-- Previous name (disabled) -->
      <input type="text" 
             name="previous" 
             value="<%= filename %>" 
             disabled
             class="border border-gray-300 rounded-lg p-2 bg-gray-100 text-gray-500 cursor-not-allowed">
      
      <!-- Hidden field to still send previous name in POST -->
      <input type="hidden" name="previous" value="<%= filename %>">

      <!-- New Name -->
      <input type="text" 
             placeholder="New Name" 
             name="new" 
             class="border border-gray-300 rounded-lg p-2 focus:outline-none focus:ring-2 focus:ring-blue-500" 
             required>

      <!-- Submit -->
      <input type="submit" 
             value="Update Task" 
             class="bg-yellow-500 text-white rounded-lg py-2 px-4 hover:bg-yellow-600 cursor-pointer">
    </form>

  </div>

</body>
</html>


